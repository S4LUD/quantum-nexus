export type ScreenRoute =
  | "/(screens)/Home"
  | "/(screens)/Game"
  | "/(screens)/Settings"
  | "/(screens)/Multiplayer/index"
  | "/(screens)/Lobby/index";

const screenRoutes: ScreenRoute[] = [
  "/(screens)/Home",
  "/(screens)/Game",
  "/(screens)/Settings",
  "/(screens)/Multiplayer/index",
  "/(screens)/Lobby/index",
];

export default screenRoutes;
